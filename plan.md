# 작업 계획.

## 2026-05-16 카탈로그.md 작성.

목표는 카탈로그 데이터가 어디에서 만들어지고, 어떤 파일로 고정되며, React 화면과 옷장 저장 로직에서 어떻게 소비되는지 협업자가 바로 이해할 수 있게 정리하는 것이다.

1. `src/data/*Catalog.ts`, `public/catalog`, `scripts/*catalog*.py`를 확인한다.
2. `src/App.tsx`의 카탈로그 표시, 선택, 미리보기, 저장, 데일리룩 사용 흐름을 확인한다.
3. `scripts/validate_catalog.py`로 현재 데이터와 이미지 매칭 상태를 확인한다.
4. `카탈로그.md`를 작성하고 생성 여부를 확인한다.

## 2026-05-16 이해도(codex).md 재작성.

목표는 기존 `이해도.md`를 참고하되, 현재 코드와 데이터가 바뀐 상태를 새 기준으로 다시 받아들여 `이해도(codex).md`에 정리하는 것이다.

1. 기존 `이해도.md`와 프로젝트 문서 목록을 확인한다.
2. `package.json`, Vite 설정, 타입 정의, 주요 React 파일, 서비스 파일, 서버 파일, 스크립트 파일을 읽어 현재 실행 구조를 파악한다.
3. 이미지 카탈로그와 생성 데이터는 파일 수와 데이터 소스 연결 방식으로 요약한다.
4. 문서를 새로 작성하고, 생성 파일과 변경된 체크리스트/맥락 기록을 확인한다.
# 2026-05-24 추천 근거 패널 1차.

목표는 추천 결과를 과장된 데모 화면으로 만들지 않고, 왜 이 코디가 추천됐는지 사용자가 바로 이해할 수 있게 하는 것이다.

1. `src/services/recommendationEngine.ts`에서 이미 계산되는 퍼스널컬러, 날씨, 조화, 안정성 점수를 카드 표시용 근거 데이터로 묶는다.
2. `src/App.tsx`의 추천 카드에 점수 분해와 핵심 근거 3개를 작게 표시한다.
3. 새 UI는 기존 추천 카드 안에만 넣고, 별도 대시보드나 큰 인터랙션은 이번 단계에서 만들지 않는다.
4. `npm test`, `npm run lint`, `npm run build`로 타입과 빌드를 확인한다.
